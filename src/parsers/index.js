const forParser = require('./forParser')
const ifParser = require('./ifParser')
const frontmatter = require('./frontmatter')
const markdown = require('./markdown')

module.exports = {
  forParser,
  ifParser,
  frontmatter,
  markdown
}
